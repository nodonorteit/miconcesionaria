version: '3.8'

services:
  app:
    image: swr.sa-argentina-1.myhuaweicloud.com/nodonorteit/miconcesionaria:latest
    platform: linux/amd64
    env_file:
      - .env.production
    ports:
      - "3000:3000"
    environment:
      - DATABASE_URL=mysql://miconcesionaria:%21FVsxr%3Fpmm34xm2N@172.17.0.1:3306/miconcesionaria
      - NEXTAUTH_URL=${NEXTAUTH_URL}
      - NEXTAUTH_SECRET=${NEXTAUTH_SECRET}
      - SMTP_HOST=${SMTP_HOST}
      - SMTP_PORT=${SMTP_PORT}
      - SMTP_USER=${SMTP_USER}
      - SMTP_PASS=${SMTP_PASS}
      - NODE_ENV=production
    volumes:
      - ./uploads:/app/uploads
    restart: unless-stopped
    # healthcheck:
    #   test: ["CMD", "wget", "--quiet", "--tries=1", "--spider", "http://localhost:3000/api/health"]
    #   interval: 30s
    #   timeout: 10s
    #   retries: 3
    # No depends_on needed - using native MariaDB

  # MariaDB service removed - using native MariaDB on server
  # Database connection will be configured via environment variables

# No volumes needed - using native MariaDB
